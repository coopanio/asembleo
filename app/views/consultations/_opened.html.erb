<div class="alert alert-primary" role="alert">
  <% if @current_user.receipts.count == @consultation.questions.count %>
    Ja has fet totes les votacions d'aquest bloc.
  <% else %>
    <p>La votació està oberta però no té preguntes actives. Prova més tard.</p>
    <% if @current_user.voter? %>
      <%= link_to 'Refrescar', { controller: 'events', action: 'next_question', id: @current_user.event_id }, class: 'btn btn-primary btn-lg' %>
    <% end %>
  <% end %>
</div>