<div class="card mt-3">
  <div class="card-header d-flex justify-content-between">
    <strong>Preguntes</strong>
    <% if @current_user.admin? %>
      <ul class="nav">
        <li class="nav-item">
          <%= link_to 'Crear', new_question_path, class: 'nav-link p-0 text-decoration-underline' %>
        </li>
      </ul>
    <% end %>
  </div>
  <div class="card-body">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Pregunta</th>
          <th>Estat</th>
          <th colspan="3"></th>
        </tr>
      <thead>
      <% @consultation.questions.order(:id).each do |question| %>
      <tr>
        <td><%= question.description %></td>
        <td><%= question.status %></td>
        <td><%= link_to 'Editar', edit_question_url(question)  %></td>
        <td><%= question_opener_closer_link(@event, question)  %></td>
        <% if question.closed? %>
          <td><%= link_to 'Resultats', { controller: :questions, action: :tally, id: question.id }, class: 'btn btn-link py-0' %></td>
        <% end %>
        <% if question.draft? %>
          <td><%= button_to 'Eliminar', question_path(question), method: :delete, class: 'btn btn-link py-0 link-danger' %></td>
        <% end %>
      </tr>
      <% end %>
    </table>
  </div>
</div>